streamlit==1.40.1

# OpenAI Python SDK (chat + vision)
openai>=1.51.0

# For loading environment variables locally (optional, but handy)
python-dotenv==1.0.1

# PDF → image conversion for calendar parsing
PyMuPDF==1.24.3
